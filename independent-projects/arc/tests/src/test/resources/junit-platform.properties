# Per-class lifecycle allows ArcTestContainer (a non-static @RegisterExtension field)
# to receive BeforeAllCallback, which is used for the reproducibility check.
# Without this, BeforeAllCallback is only invoked for static extension fields.
junit.jupiter.testinstance.lifecycle.default=per_class
